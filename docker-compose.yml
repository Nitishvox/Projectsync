services:
  project-sync:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: project-sync-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    env_file:
      - .env
